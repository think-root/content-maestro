services:
  content-maestro:
    container_name: content-maestro
    restart: always
    image: content-maestro:latest
    build:
      context: .
      dockerfile: Dockerfile
      args:
        APP_VERSION: ${APP_VERSION}
    env_file:
      - .env
    environment:
      - TZ=Europe/Kiev
      - APP_VERSION=${APP_VERSION}
    ports:
      - "${API_PORT:-8080}:${API_PORT:-8080}"
    volumes:
      - badger-data:/app/data/badger
    networks:
      - think-root-network

volumes:
  badger-data:

networks:
  think-root-network:
    external: true